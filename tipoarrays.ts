//los arreglos pueden ser usando [] llamado arreglo tipo definido
//el siguiente arreglo solo puede tener números
let listaDeNumeros: number[] = [1, 2, 3];
//listaDeNumeros.push("a"); //esto dará error

//otra forma de crear un arreglo es usando Array<number>
let listaDeNumerosdos: Array<number> = [1, 2, 3];
//imprimo arreglo con []
console.log("arreglo numerico[]");
console.log(listaDeNumeros);
//lista con Array<>
console.log("array<number>")
console.log(listaDeNumerosdos);